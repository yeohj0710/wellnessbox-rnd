# OP-115 두 저장소의 단위·통합·E2E·인코딩·빌드 검사를 통과한다.

## 결론

OP-115는 요구 단계 `INTEGRATED`에 도달했다. R&D 저장소의 FastAPI 배포 계약 테스트 15건과 실제 프로세스 재시작 smoke 8건이 통과했다. WellnessBox 저장소에서는 R&D health alias QA, TypeScript 타입 검사, 인코딩 감사, Next.js 프로덕션 빌드가 모두 통과했다. 검사는 구현 파일을 읽는 정적 확인에 그치지 않고 FastAPI 프로세스, HTTP health 응답, Next.js route가 사용하는 서버 함수를 실행했다.

## 검사 범위와 기존 시스템 재사용

R&D는 기존 `tests/test_deployment_contract.py`와 `scripts/run_deployment_contract_endpoint_smoke.py`를 재사용했다. 이 경로는 health, 추천, 상태 기계, 기기, 상담 API를 한 프로세스에 마운트하고 영속 SQLite를 사용한 재시작까지 검사한다. WellnessBox는 기존 환경 계약 `lib/server/wb-rnd-environment.ts`를 그대로 사용했다. 새 health 기능만 별도 클라이언트로 추가했으며 추천·관리자 API 클라이언트를 복제하지 않았다.

단위·통합·E2E라는 이름만 붙인 빈 검사는 사용하지 않았다. 배포 계약의 실패 폐쇄 조건은 pytest가 검증하고, 프로세스 smoke는 실제 HTTP 소켓과 재시작 데이터 보존을 검증한다. WellnessBox health QA는 임시 HTTP 서버를 세워 실제 fetch 요청 경로와 정상·비활성 응답을 검증한다. `npm run build`는 새 `/api/internal/rnd/health` route를 포함한 프로덕션 번들을 생성했다.

## 오류 입력과 경계 동작

R&D 배포 계약은 짧은 내부 토큰, 상대 DB 경로, 다중 SQLite worker, 누락된 provider secret reference를 거부한다. WellnessBox alias는 R&D 연동이 꺼져 있으면 503 `disabled`, upstream 호출이나 JSON 처리가 실패하면 503 `unavailable`을 반환한다. 공개 응답에는 토큰, upstream URL, 예외 stack을 넣지 않는다. 정상 응답도 upstream의 전체 health 문서를 그대로 노출하지 않고 상태, 환경, 배포 준비 여부만 전달한다.

## canonical evidence 생성과 재현 범위

동결 데이터셋 `data/original_plan/op115_op116_test_matrix_health_alias_cases_v1.json`은 두 저장소의 검사 계층과 health 왕복을 8건으로 고정한다. canonical evidence는 실행한 검사 결과, 관측값, WellnessBox 커밋과 관련 Git blob을 기록한다. 같은 커밋에서 각 명령을 다시 실행하면 코드와 결과의 대응을 확인할 수 있다. 로컬 `.env` 값이나 토큰은 evidence에 기록하지 않았다.

## 단계 경계와 남은 조건

OP-115의 필수 단계는 저장소 간 통합 검사 통과이므로 현재 증거로 충족한다. 다만 이 보고서는 장기간 부하, 실제 provider 장애, 프로덕션 트래픽을 증명하지 않는다. 그 항목은 OP-116 이후의 운영 증거로 별도 관리한다.

## 관련 검증

- R&D: `python -m pytest tests/test_deployment_contract.py -q` — 15 passed
- R&D: `python scripts/run_deployment_contract_endpoint_smoke.py` — 8 cases, 모든 check true
- WellnessBox: `npm run qa:rnd:health-alias` — passed
- WellnessBox: `npm run audit:encoding` — passed
- WellnessBox: `npm run typecheck` — passed
- WellnessBox: `npm run build` — passed, 정적 페이지 99개 생성
- WellnessBox 소스 커밋: `b37bf99a8f2a5a7eb50fe61016740579011d2aa3`
## 운영 증거와 종료 판단을 나눈 이유
운영 증거는 구현 파일을 나열하는 데서 끝나지 않는다. 보안, 장애 복구, 계약 일치, 배포 health, 브라우저 경로, 운영 증거, 외부 의존성과 최종 감사가 각각 어떤 주장을 지지하는지 단계별로 나눈다.

`OP-115`는 “두 저장소의 단위·통합·E2E·인코딩·빌드 검사를 통과한다.”를 다루는 항목이다. 이 항목을 읽을 때는 파일이 존재한다는 사실과 요구된 동작이 증명됐다는 사실을 구분한다. 아래 근거는 이 보고서의 기존 설명을 다시 확인할 때 먼저 열어야 할 자료다.

### 근거를 읽는 순서
- 원문 출처: `docs/context/original_plan.pdf#page=15-26`
- 구현·설계: `wellnessbox-rnd/apps/inference_api/routes/health.py`, `wellnessbox/app/api/internal/rnd/health/route.ts`, `wellnessbox/lib/server/wb-rnd-health.ts`
- 테스트·실행: `wellnessbox-rnd/tests/test_deployment_contract.py`, `wellnessbox-rnd/scripts/run_test_matrix_health_alias_smoke.py`, `wellnessbox/scripts/qa/check-rnd-health-alias.cts`, `wellnessbox-rnd/data/original_plan/evidence/op115_op116_test_matrix_health_alias_smoke_v1.json`
- 통합 증거: `wellnessbox-rnd/data/original_plan/evidence/op115_op116_test_matrix_health_alias_smoke_v1.json`

구현 파일은 설계와 코드의 위치를 알려 주고, 테스트 파일은 어떤 분기를 실행했는지 알려 준다. 통합·운영·외부 증거가 따로 적혀 있지 않다면 구현 파일만으로 그 단계를 주장하지 않는다. 이 구분이 있어야 연구보고서가 “코드가 있다”와 “실제 연구개발 과정에서 검증됐다”를 섞지 않는다.

### 현재 단계와 해석의 경계
manifest는 요구 단계를 `INTEGRATED`, 현재 주장 단계를 `INTEGRATED`로 기록한다.
두 단계 표기는 일치하지만, 이 값이 다른 OP의 운영·외부 검증까지 자동으로 승격시키지는 않는다. 이 보고서는 연결된 증거가 직접 말하는 범위까지만 결론을 쓴다.

이번 보강은 새로운 실험 결과, 사람의 판정·승인·서명, 운영 트래픽을 추가하지 않는다. 기존 연구보고서의 사실과 증거를 더 쉽게 읽도록 연구 질문, 확인 순서, 단계 경계를 풀어 쓴다.
