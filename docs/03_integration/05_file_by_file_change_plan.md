# archived: file by file change plan

상태: `ARCHIVE_AS_SCOPE_DRIFT`

이 문서는 `wellnessbox` 특정 파일 경로를 기준으로 한 과거 변경 계획이다. 현재 독립 R&D 실행 범위에서는 사용하지 않는다.

현재 다음 단계는 제품 파일별 변경이 아니라 다음 순서다.

1. KPI / 요구사항 재정리
2. schema refinement
3. frozen eval 확대
4. deterministic baseline 고도화
5. safety / efficacy / ranking 강화
